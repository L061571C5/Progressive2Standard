# Progressive2Standard
A utility to convert progressive jpeg's to standard for the music player OS Rockbox
## Usage
Install dependencies with `npm install` then run `node index.js "INSERTDIRECTORYHERE"`.
